{
  "name": "@proficiency/api",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "uvicorn src.main:app --reload --host 0.0.0.0 --port 8000",
    "lint": "echo \"backend lint placeholder\"",
    "test": "python -m pytest"
  }
}
