{
  "name": "proficiency-faculty-evaluation",
  "version": "0.0.1",
  "private": true,
  "packageManager": "pnpm@9.11.0",
  "scripts": {
    "lint": "echo \"lint placeholder\"",
    "test": "echo \"test placeholder\"",
    "db:migrate": "docker compose run --rm api alembic upgrade head"
  }
}
